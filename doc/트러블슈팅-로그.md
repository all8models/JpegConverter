# 🐛 이미지 압축 서비스 - 트러블슈팅 로그

> **날짜**: 2026-05-27
> **프로젝트**: JpegDownload (이미지 압축 웹서비스)

---

## 1차 이슈: 압축 70%에서 멈춤

### 증상
- http://localhost:5173 접속, 이미지 파일 업로드 후 "이미지 압축 중..." 메시지와 함께 **70% 진행중에 멈춤**
- 오류 메시지 표시 없이 무한 대기

### 원인 분석
- `image_tasks.py`의 `compress_image_task`에서 70% progress 설정 후 **MinIO 업로드 단계에서 예외 발생**
- MinIO 연결 실패 / hang 발생 시 `try-except`가 없어서 태스크가 조용히 실패
- Celery 태스크는 `FAILURE` 상태로 전환되었으나, 프론트엔드 폴링에서 `catch {}`로 오류를 무시 → UI가 멈춘 것처럼 보임

### 작업자(Worker) 로그 (정상 완료 케이스 - 이후 테스트)
```
[2026-05-27 06:25:29] Task compress_image_task[35060930-c662-4f67-83e5-0fc19de81ede] received
[2026-05-27 06:25:31] Task compress_image_task[35060930-c662-4f67-83e5-0fc19de81ede] succeeded in 1.69s:
  'task_id': '35060930-c662-4f67-83e5-0fc19de81ede',
  'original_size': 712724,
  'compressed_size': 317116,
  'ratio': 2.2,
  'original_info': {'width': 2560, 'height': 1600, 'format': 'JPEG', 'mode': 'RGB', 'size': 712724},
  'filename': 'compressed.jpg'
```

### 조치 내용

#### ① MinIO 의존성 제거 → 로컬 파일 시스템 사용
**파일**: `backend/app/tasks/image_tasks.py`

- `minio` 패키지 import 제거 (선택적 import로 변경)
- 로컬 파일 저장 함수 `save_to_local()` 추가
- `STORAGE_BACKEND` 설정에 따라 local / minio 분기 처리
- 전체 태스크를 `try-except`로 감싸 예외 발생 시 `FAILURE` 상태 + 오류 메시지 전파
- `traceback` 로깅 추가

```python
def save_to_local(task_id, compressed_bytes, output_format):
    task_dir = os.path.join(settings.RESULT_DIR, task_id)
    os.makedirs(task_dir, exist_ok=True)
    filepath = os.path.join(task_dir, f"compressed.{file_ext}")
    with open(filepath, "wb") as f:
        f.write(compressed_bytes)
    return filepath, filename

# MinIO는 STORAGE_BACKEND == "minio" 일 때만 동적 import
```

#### ② API 서버 다운로드 엔드포인트 수정
**파일**: `backend/app/api/compress.py`

- `STORAGE_BACKEND` 설정에 따라 로컬 파일 / MinIO 분기
- `result.get("original_size", 0)` 등 `.get()` 으로 방어적 접근
- `FAILURE` 상태 시 `task.info`에서 `error` 키 추출하여 상세 메시지 전달

#### ③ Docker 공유 볼륨 추가
**파일**: `docker-compose.yml`

- backend ↔ worker 간 `/tmp/results` 공유 볼륨(`results-data`) 추가
- `STORAGE_BACKEND=local`, `RESULT_DIR=/tmp/results` 환경변수 설정
- `VITE_BACKEND_HOST=backend` 환경변수 추가

```yaml
volumes:
  - results-data:/tmp/results  # worker와 backend가 같은 디렉토리 공유
```

#### ④ 설정 파일 업데이트
**파일**: `backend/app/core/config.py`
- `STORAGE_BACKEND: str = "local"` 추가

**파일**: `backend/.env`
- `STORAGE_BACKEND=local`, `RESULT_DIR=/tmp/results` 추가
- MinIO 관련 설정 제거

---

## 2차 이슈: 압축 10%에서 멈춤

### 증상
- 1차 수정 후 재배포하였으나 **같은 증상, 이번에는 10%에서 멈춤**

### 원인 분석

#### 백엔드(API) 로그
```
File "/app/app/api/compress.py", line 80, in get_status
    original_size=result["original_size"],
KeyError: 'original_size'
```

- Celery 태스크 자체는 **정상 완료** (worker 로그에서 `succeeded` 확인)
- 그러나 API 서버에서 `AsyncResult(task_id).result` 로 결과 조회 시 `result["original_size"]` 접근에서 `KeyError` 발생
- `Celery result_backend`(Redis)에 저장된 결과를 가져올 때 serialization/deserialization 과정에서 dict 키가 유실되었거나 예상과 다른 형식으로 반환됨

#### 프론트엔드 오류 처리 문제
- `useCompression.ts` 폴링 루프에서 `catch {}`로 **모든 오류를 무시**
- API가 500 에러를 반환해도 프론트엔드는 무시 → 마지막 정상 progress(10%)에 멈춘 것처럼 보임
- 사용자에게 오류 메시지가 전혀 표시되지 않음

### 동작 흐름 추적
```
1. 프론트엔드 → POST /api/compress (파일 업로드)
2. API 서버 → Celery task.delay() → Redis에 태스크 등록
3. Worker → 태스크 수신 → 압축 수행 (0.4초 내 완료) → 결과 Redis에 저장
4. 프론트엔드 → GET /api/status/{task_id} (1초 간격 폴링)
   ├─ 1회차: state=PROCESSING, progress=10 → UI: 10% 표시
   ├─ 2회차: state=SUCCESS → result["original_size"] KeyError → 500 응답
   │          프론트 catch {} → 무시 → UI 여전히 10%
   └─ 3회차 이후: 계속 500 → 계속 무시 → 영원히 10% 멈춤
```

### 조치 내용

#### ① 상태 확인 API 방어적 코드 적용
**파일**: `backend/app/api/compress.py`

```python
elif task.state == "SUCCESS":
    try:
        result = task.result
        logger.info(f"Task {task_id} SUCCESS, result type={type(result).__name__}, ...")
        if not isinstance(result, dict):
            result = {}
        return CompletedResponse(
            status="completed",
            download_url=f"/api/download/{task_id}",
            original_size=result.get("original_size", 0),   # [] → .get()
            compressed_size=result.get("compressed_size", 0),
            ratio=result.get("ratio", 0),
        )
    except Exception as e:
        return ErrorResponse(status="error", message=f"결과 조회 실패: {str(e)}")
```

- `result["key"]` → `result.get("key", default)` 방어적 접근
- `isinstance(result, dict)` 타입 체크 추가
- 전체 try-except로 예외 캡처
- 디버깅 로깅 추가 (`logger.info`)

#### ② 프론트엔드 폴링 오류 처리 개선
**파일**: `frontend/src/hooks/useCompression.ts`

```typescript
// 변경 전: 모든 오류 무시
} catch {
    // 폴링 계속 진행
}

// 변경 후: 3회 연속 오류 시 사용자에게 알림
let errorCount = 0;
} catch (err: any) {
    errorCount++;
    if (errorCount >= 3) {
        stopPolling();
        setState((prev) => ({
            ...prev,
            status: 'error',
            error: '서버 연결이 원활하지 않습니다. 다시 시도해주세요.',
        }));
    }
}
```

---

## 수정된 파일 목록

| # | 파일 | 변경 유형 | 비고 |
|---|------|-----------|------|
| 1 | `backend/app/tasks/image_tasks.py` | 구조 변경 | MinIO 제거 → 로컬 파일 저장, try-except 추가 |
| 2 | `backend/app/api/compress.py` | 수정 | 방어적 코드, 로깅, 로컬 다운로드 지원 |
| 3 | `backend/app/core/config.py` | 추가 | `STORAGE_BACKEND` 설정 |
| 4 | `backend/.env` | 수정 | `STORAGE_BACKEND=local` |
| 5 | `docker-compose.yml` | 수정 | 공유 볼륨, 환경변수 |
| 6 | `frontend/vite.config.ts` | 수정 | `VITE_BACKEND_HOST` 환경변수 |
| 7 | `frontend/src/hooks/useCompression.ts` | 수정 | 폴링 오류 3회 허용 후 에러 표시 |

---

## 아키텍처 변경 사항

### 변경 전
```
Worker ──MinIO 저장──▶ MinIO 컨테이너
API    ──MinIO 읽기──▶ MinIO 컨테이너
```
→ MinIO 연결 불안정 시 전체 서비스 중단

### 변경 후
```
Worker ──파일 저장──▶ /tmp/results (공유 볼륨)
API    ──파일 읽기──▶ /tmp/results (공유 볼륨)
```
→ Docker named volume으로 안정적 파일 공유
→ `STORAGE_BACKEND=minio` 설정 시 MinIO로 전환 가능

---

## 향후 개선 제안

1. **Celery 결과 직렬화 이슈 근본 해결**: `result["original_size"]` KeyError의 정확한 원인을 파악하려면 API 로그의 `logger.info` 출력을 확인 필요
2. **파일 업로드 방식 개선**: 현재는 이미지 바이너리 전체를 Celery 메시지로 전달 → 대용량 파일 비효율적 → 디스크에 먼저 저장 후 경로만 전달
3. **MinIO 사용 시**: `STORAGE_BACKEND=minio` + MinIO 연결 타임아웃 설정 추가
4. **Docker Compose `version` 필드 제거**: Docker Compose V2에서 deprecated 경고 제거

---

## 3차 이슈: 압축 결과가 모두 0으로 표시됨

### 증상
- 압축은 완료되나 결과 화면에서 **원본 크기=0, 압축 후=0, 압축률=0** 으로 표시

### 원인 분석

#### Redis 저장 데이터 직접 확인
```bash
$ docker exec img-compress-redis redis-cli GET "celery-task-meta-d3fb1d3a-..."
```

결과:
```json
{"status": "SUCCESS", "result": {"progress": 100}}
```

**실제 태스크 반환값이 저장되지 않고, 마지막 `self.update_state()`의 meta만 저장됨**

#### Celery `self.update_state()` 동작 방식
Celery에서 `bind=True` 태스크가 `self.update_state(state="SUCCESS", meta={...})`를 호출하면, 이 호출이 **결과 저장소에 최종 결과로 기록**된다. 이후 `return result_dict`를 해도, 이미 state가 "SUCCESS"로 설정되었기 때문에 **return 값은 무시**된다.

```python
# 잘못된 코드 (이번 버그의 원인)
self.update_state(state="SUCCESS", meta={"progress": 100})  # ← Redis에 {"progress": 100} 저장
return result  # ← 이 값은 무시됨 (이미 SUCCESS 상태)
```

### 조치 내용

**파일**: `backend/app/tasks/image_tasks.py`

```python
# 변경 전
self.update_state(state="SUCCESS", meta={"progress": 100})
return result

# 변경 후
# self.update_state(state="SUCCESS", ...) 제거
# return 값이 자동으로 SUCCESS 결과로 Redis에 저장됨
return result
```

- `state="SUCCESS"`인 `update_state` 호출 제거
- Celery가 태스크 return 값을 자동으로 결과로 저장하도록 수정
- 마지막 progress 업데이트는 `state="PROCESSING"`으로 유지

---

## Celery `update_state` vs `return` 정리

| 방식 | 동작 | Redis 저장 결과 |
|------|------|----------------|
| `self.update_state(state="SUCCESS", meta=X)` + `return Y` | `update_state`가 우선, `return` 무시 | `X` |
| `return Y` 만 사용 | return 값이 자동 저장 | `Y` |
| `self.update_state(state="PROCESSING", meta=X)` + `return Y` | 진행률만 X, 최종 결과는 Y | `Y` ✅ |
