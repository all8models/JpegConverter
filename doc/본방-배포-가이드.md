# 🚀 이미지 압축 웹서비스 — 본방 배포 가이드

> **작성일**: 2026-05-27
> **프로젝트**: JpegDownload

---

## 1. 본방 아키텍처 개요

```
┌───────── Internet ─────────┐
          │ :443
    ┌─────▼──────┐
    │  Reverse   │  ← Nginx / Caddy (SSL termination)
    │  Proxy     │
    └─────┬──────┘
          │
    ┌─────▼──────────────────────────────┐
    │         Docker Compose             │
    │                                    │
    │  ┌──────────┐  ┌────────────────┐  │
    │  │ Frontend │  │   Backend API  │  │
    │  │ (Nginx)  │  │  (Gunicorn +   │  │
    │  │ :80      │  │   Uvicorn)     │  │
    │  └──────────┘  │   :8000        │  │
    │                └───────┬────────┘  │
    │  ┌──────────┐         │           │
    │  │  Redis   │◀────────┘           │
    │  │  :6379   │                     │
    │  └──────────┘                     │
    │                        ┌────────┐ │
    │                        │ Celery │ │
    │                        │ Worker │ │
    │                        └────────┘ │
    └────────────────────────────────────┘
```

---

## 2. 본방에 필요한 파일 리스트

### 2.1 서버에 반드시 있어야 할 파일 (소스)

```
jpegdownload/
├── docker-compose.prod.yml       # 본방용 Docker Compose (핵심)
├── .env                          # 본방 환경변수 (서버에서 직접 작성)
│
├── backend/
│   ├── Dockerfile.prod           # 본방용 Dockerfile
│   ├── requirements.txt
│   └── app/                      # 전체 애플리케이션 코드
│       ├── __init__.py
│       ├── main.py
│       ├── api/
│       │   ├── __init__.py
│       │   └── compress.py
│       ├── core/
│       │   ├── __init__.py
│       │   └── config.py
│       ├── models/
│       │   ├── __init__.py
│       │   └── schemas.py
│       ├── services/
│       │   ├── __init__.py
│       │   └── compressor.py
│       └── tasks/
│           ├── __init__.py
│           └── image_tasks.py
│
├── frontend/
│   ├── Dockerfile.prod           # 본방용 Dockerfile (멀티스테이지 빌드)
│   ├── nginx.conf.prod           # 본방용 Nginx 설정
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   ├── index.html
│   └── src/                      # 전체 프론트엔드 소스
│       ├── main.tsx
│       ├── App.tsx
│       ├── api/compress.ts
│       ├── hooks/useCompression.ts
│       ├── components/
│       │   ├── FileUploader.tsx
│       │   ├── QualitySlider.tsx
│       │   ├── ProgressBar.tsx
│       │   └── CompressionResult.tsx
│       └── styles/App.css
│
└── .gitignore
```

### 2.2 서버에서 실행 전에 생성해야 할 파일

| 파일 | 생성 방법 | 설명 |
|------|-----------|------|
| `.env` | 수동 작성 | DB 비밀번호 등 민감 정보 |
| `docker-compose.prod.yml` | 템플릿 기반 수정 | 도메인, 포트 등 환경에 맞게 |
| `backend/Dockerfile.prod` | 아래 템플릿 참고 | Gunicorn 기반 |
| `frontend/nginx.conf.prod` | 아래 템플릿 참고 | 리버스 프록시 + 정적 파일 |

### 2.3 서버에 있으면 안 되는 파일

```
❌ backend/.env              → 개발용 로컬 설정 (본방 .env로 대체)
❌ frontend/node_modules/    → Docker 빌드 시 생성
❌ frontend/dist/            → Docker 빌드 시 생성
❌ backend/__pycache__/      → 실행 시 자동 생성
❌ *.md 문서 파일             → 개발 문서 (서버 불필요)
```

---

## 3. 본방 배포 순서 (총 10단계)

### Step 1: 본방 서버 준비

```bash
# 1-1. 서버 접속 (예: Ubuntu 24.04 LTS)
ssh root@your-server-ip

# 1-2. Docker 및 Git 설치
apt update && apt upgrade -y
apt install -y docker.io docker-compose-v2 git

# 1-3. 프로젝트 디렉토리 생성
mkdir -p /opt/jpegdownload
cd /opt/jpegdownload
```

### Step 2: 소스 코드 배포

```bash
# GitHub 등에서 클론
git clone https://github.com/your-org/jpegdownload.git .

# 또는 SCP/SFTP로 직접 업로드
# rsync -avz --exclude='.git' --exclude='node_modules' ./ user@server:/opt/jpegdownload/
```

### Step 3: 본방 환경변수 설정

```bash
# .env 파일 생성 (민감 정보 포함)
cat > .env << 'ENVEOF'
APP_NAME=Image Compression Service
DEBUG=false

# Celery Redis
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# 스토리지 (local 또는 minio)
STORAGE_BACKEND=local
RESULT_DIR=/tmp/results

# MinIO 사용 시 (선택)
# MINIO_ENDPOINT=minio:9000
# MINIO_ACCESS_KEY=변경하세요
# MINIO_SECRET_KEY=변경하세요
ENVEOF

chmod 600 .env  # 권한 제한
```

### Step 4: 본방용 Dockerfile 준비

**`backend/Dockerfile.prod`** — Gunicorn + Uvicorn 워커 사용:

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt gunicorn

COPY . .

EXPOSE 8000

# Gunicorn으로 Uvicorn 워커 실행 (reload 없음, production 모드)
CMD ["gunicorn", "app.main:app", \
     "--worker-class", "uvicorn.workers.UvicornWorker", \
     "--bind", "0.0.0.0:8000", \
     "--workers", "4", \
     "--max-requests", "10000", \
     "--max-requests-jitter", "500", \
     "--timeout", "120", \
     "--access-logfile", "-", \
     "--error-logfile", "-"]
```

**`frontend/Dockerfile.prod`** — 기존 `Dockerfile`과 동일 (멀티스테이지):

```dockerfile
FROM node:22-alpine AS build
WORKDIR /app
COPY package.json .
RUN npm ci --only=production
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf.prod /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Step 5: 본방용 Nginx 설정

**`frontend/nginx.conf.prod`**:

```nginx
# upstream = Docker 내부의 backend 서비스
upstream backend_api {
    server backend:8000;
}

server {
    listen 80;
    server_name your-domain.com;   # ← 실제 도메인으로 변경

    # Gzip 압축
    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;
    gzip_min_length 1000;

    # 정적 파일 (React 빌드 결과물)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "public, max-age=3600";
    }

    # API 라우트 (리버스 프록시)
    location /api {
        proxy_pass http://backend_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 25M;
        proxy_read_timeout 120s;
    }

    # 상태 체크
    location /health {
        proxy_pass http://backend_api/health;
    }
}
```

### Step 6: 본방용 Docker Compose

**`docker-compose.prod.yml`**:

```yaml
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: jpg-backend
    expose:
      - "8000"              # 내부 포트만 (외부 직접 노출 ❌)
    volumes:
      - results-data:/tmp/results
    env_file: .env
    depends_on:
      redis:
        condition: service_started
    restart: unless-stopped
    # 리소스 제한 (선택)
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: jpg-worker
    command: celery -A app.tasks.image_tasks.celery_app worker --loglevel=info --concurrency=2
    volumes:
      - results-data:/tmp/results
    env_file: .env
    depends_on:
      redis:
        condition: service_started
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G

  redis:
    image: redis:7-alpine
    container_name: jpg-redis
    expose:
      - "6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    deploy:
      resources:
        limits:
          memory: 512M

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    container_name: jpg-frontend
    ports:
      - "80:80"              # 80번 포트로 서빙
    depends_on:
      - backend
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M

volumes:
  results-data:
  redis-data:
```

> **기존 `docker-compose.yml` 과의 차이점**:
> - `ports` 대신 `expose` 사용 (직접 노출 ❌, 프록시를 통해서만 접근)
> - `env_file`로 `.env` 파일 직접 로드
> - 리소스 제한(`deploy.resources`) 추가
> - Redis AOF 활성화 (데이터 영속성)
> - MinIO 제거 (본방에서도 local 스토리지 사용)
> - `restart: unless-stopped`로 자동 복구

### Step 7: SSL/HTTPS 설정

```bash
# 방법 A: Caddy (자동 SSL, 가장 간단)
cat > /opt/jpegdownload/Caddyfile << 'EOF'
your-domain.com {
    reverse_proxy frontend:80
}
EOF

docker run -d \
  --name jpg-caddy \
  -p 443:443 -p 80:80 \
  -v /opt/jpegdownload/Caddyfile:/etc/caddy/Caddyfile \
  -v caddy-data:/data \
  --network jpegdownload_default \
  caddy:2

# 방법 B: Nginx + Certbot (수동)
# apt install -y nginx certbot python3-certbot-nginx
# certbot --nginx -d your-domain.com
```

### Step 8: 실행 및 검증

```bash
# 8-1. 이미지 빌드 및 실행
docker compose -f docker-compose.prod.yml up -d --build

# 8-2. 컨테이너 상태 확인
docker compose -f docker-compose.prod.yml ps

# 8-3. 로그 확인
docker compose -f docker-compose.prod.yml logs --tail=50

# 8-4. 헬스 체크
curl http://localhost/health
# {"status":"ok","service":"Image Compression Service"}

# 8-5. 각 서비스 상태 개별 확인
docker logs jpg-worker --tail 20   # Celery worker 준비 확인
docker logs jpg-redis --tail 5     # Redis 연결 확인
```

### Step 9: 모니터링 설정 (선택)

```bash
# Docker 컨테이너 자동 재시작 확인
docker update --restart unless-stopped jpg-backend jpg-worker jpg-redis jpg-frontend

# 간단한 uptime 모니터링 스크립트 (cron 등록)
cat > /opt/jpegdownload/health-check.sh << 'SCRIPT'
#!/bin/bash
curl -sf http://localhost/health > /dev/null || {
    echo "[$(date)] API down. Restarting..." >> /var/log/jpg-monitor.log
    cd /opt/jpegdownload && docker compose -f docker-compose.prod.yml restart
}
SCRIPT
chmod +x /opt/jpegdownload/health-check.sh

# crontab -e
# */5 * * * * /opt/jpegdownload/health-check.sh
```

### Step 10: 도메인 연결 및 최종 확인

```
DNS 설정: your-domain.com → 서버 IP (A 레코드)

최종 확인:
1. https://your-domain.com 접속
2. 이미지 업로드 및 압축 테스트
3. 다운로드 정상 확인
4. 크롬 개발자도구 → Network 탭 → API 응답 확인
```

---

## 4. 서버 최소 사양

| 구성 | CPU | RAM | 디스크 | 예상 동시 처리 |
|------|-----|-----|--------|---------------|
| **최소** | 1코어 | 2GB | 20GB | 2~4개 파일 동시 |
| **권장** | 2코어 | 4GB | 50GB | 8~10개 파일 동시 |
| **고성능** | 4코어 | 8GB | 100GB | 20개+ 파일 동시 |

---

## 5. 배포 전 체크리스트

- [ ] `docker-compose.prod.yml`에서 `ports`가 아닌 `expose` 사용 확인
- [ ] `.env` 파일에 실제 비밀번호/키 설정
- [ ] `nginx.conf.prod`의 `server_name`을 실제 도메인으로 변경
- [ ] `client_max_body_size`가 25M로 설정되어 있는지 확인
- [ ] Celery Worker의 `--concurrency`를 서버 코어 수에 맞게 조정
- [ ] Gunicorn의 `--workers`를 `(CPU 코어 수 × 2) + 1` 로 계산
- [ ] 방화벽에서 80/443 포트만 열려 있는지 확인 (6379, 8000 등 ❌)
- [ ] SSL 인증서가 정상 발급되었는지 확인
- [ ] Git에서 민감 파일(.env 등)이 push되지 않도록 `.gitignore` 확인

---

## 6. 배포 후 긴급 대응

### 컨테이너 재시작
```bash
docker compose -f docker-compose.prod.yml restart
```

### 로그 확인
```bash
docker compose -f docker-compose.prod.yml logs -f --tail=100
```

### 특정 서비스만 재빌드
```bash
docker compose -f docker-compose.prod.yml up -d --build backend
```

### 전체 중지
```bash
docker compose -f docker-compose.prod.yml down
```

### 디스크 정리 (오래된 이미지/볼륨)
```bash
docker system prune -f
```

---

## 7. 개발 환경 vs 본방 환경 비교

| 항목 | 개발 환경 | 본방 환경 |
|------|-----------|-----------|
| **Compose 파일** | `docker-compose.yml` | `docker-compose.prod.yml` |
| **API 서버** | `uvicorn --reload` | `gunicorn + uvicorn workers` |
| **프론트엔드** | Vite dev server (핫 리로드) | Nginx 정적 파일 서빙 |
| **포트 노출** | 모든 서비스 직접 노출 | 프록시(Nginx/Caddy)만 노출 |
| **Redis** | 기본 설정 | AOF 활성화, 메모리 제한 |
| **환경변수** | `environment:` 필드 | `.env` 파일 |
| **리소스 제한** | 없음 | CPU/Memory limit 설정 |
| **재시작** | 수동 | `unless-stopped` 자동 재시작 |
| **MinIO** | 포함 | 제거 (로컬 스토리지) |
| **SSL** | 없음 (http) | 필수 (https) |
